spring:
  application:
    name: ingestData

  data:
    mongodb:
      uri: mongodb://admin:admin123@localhost:27017/testSensor?authSource=admin

server:
  port: 8180

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
